---
title: News
layout: default
---
.span-14.append-2
  - for post in news_posts[0,2]
    .post
      %h1.title
        %a{:href=>post.url}= post.title
      .byline
        .author
          = post.author
        .date
          #{post.date.strftime( '%d %B %Y' )}
      .body
        = post.body

.span-8.last
  .box
    %h1
      Older news?
    %p
      You can 
      %a{:href=>'/news/archives'} browse the full archives
      of older news and release announcements.
    
